extends layout

block content

	h1 Genre: #{genre.name}

	h4 Books

	dl
	each book in genre_books
		dt
			a(href=book.url) #{book.title}
		dd #{book.summary}

	else
		p.text-danger This genre has no books